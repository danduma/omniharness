# Ad Hoc Request

Original command:

> So we have been busy implementing a plan to reach feature parity between the desktop version and mobile version. However, I have noticed that a lot of the edits are in the rust core. We wanted to keep the ability to sync our fork with the trunk. How likely do you think this will be now that we have been editing rust?


## Supervisor Notes

This file stores the original request and any attached context for the supervisor.
