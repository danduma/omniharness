# Ad Hoc Request

Original command:

> I feel like we shouldnt show the same inline terminal window as we do in the sidebar. It's confusing. Let's keep @src/components/Terminal.tsx as is in the sidebar and make a separate control to represent a worker in the supervisor conversation that only shows when worker is started and only displays the latest action from the agent, not the full history. Otherwise worker can be mentioned when needed and when clicking on that we are taken to the worker sidebar. The sidebar should open by default when clicking


## Supervisor Notes

This file stores the original request and any attached context for the supervisor.
