# Ad Hoc Request

Original command:

> I just had a direct control session where I sent an initial message the agent did some stuff then I sent a follow-up message and the agent did more stuff. All of that worked. The problem was, when sending the second message, in the UI it looked like the second message was constantly sending, even though the agents received it and started working . At the end the second message ends up being displayed in the conversation window just under the first message, instead of interspersed in the conversation where it belongs


## Supervisor Notes

This file stores the original request and any attached context for the supervisor.
