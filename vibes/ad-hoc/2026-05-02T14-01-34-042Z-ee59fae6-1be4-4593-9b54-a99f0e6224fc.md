# Ad Hoc Request

Original command:

> we need to add the capability to past images into the chat, as well as add them from the + button, which should only trigger a file selector now instead, and we can select any file. Depending on the files added we'll try to visualize: images, text (as file name). Each file pill added can be removed with an X. We'll grow the size of the input box to double when adding files


## Supervisor Notes

This file stores the original request and any attached context for the supervisor.
