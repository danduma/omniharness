# Password auth for the web UI and phone pairing.
# Use OMNIHARNESS_AUTH_PASSWORD for a simple local setup.
# OMNIHARNESS_AUTH_PASSWORD=change-me
#
# Prefer OMNIHARNESS_AUTH_PASSWORD_HASH if this env file may be viewed by others.
# OMNIHARNESS_AUTH_PASSWORD_HASH=

# Optional: private origin for phone pairing and remote access links.
# `pnpm setup:tailscale` manages this plus the loopback runner host automatically.
# OMNIHARNESS_PUBLIC_ORIGIN=https://your-mac.your-tailnet.ts.net

# Optional public HTTP chat API. Requests may access only this fixed project
# and must supply this value as `Authorization: Bearer <key>`.
# OMNIHARNESS_PUBLIC_API_KEY=
# OMNIHARNESS_PUBLIC_API_PROJECT_PATH=/Users/you/code/project
# Optional multi-project form. When set, this replaces PUBLIC_API_PROJECT_PATH.
# OMNIHARNESS_PUBLIC_API_PROJECTS=[{"id":"app","path":"/Users/you/code/app"}]

# Runner/API deployment. The ACP bridge remains loopback by default.
# OMNIHARNESS_RUNNER_HOST=127.0.0.1
# OMNIHARNESS_RUNNER_PORT=3050
# OMNIHARNESS_BRIDGE_URL=http://127.0.0.1:7800
# OMNIHARNESS_MANAGE_BRIDGE=1
#
# Isolated data root/instance. Each live instance needs unique ports.
# OMNIHARNESS_ROOT=/srv/omniharness
# OMNIHARNESS_INSTANCE=primary
#
# Static interface: default is dist/interface. Set one of these, never both.
# OMNIHARNESS_STATIC_DIR=/opt/omniharness/interface
# OMNIHARNESS_NO_STATIC=1
#
# Trust forwarding headers only from exact proxy peers or narrow IPv4 CIDRs.
# OMNIHARNESS_TRUSTED_PROXIES=127.0.0.1,10.20.0.0/16
#
# Directory trees the folder picker and project file reader may reach, separated
# by the platform path delimiter. Defaults to the parent of the runner's working
# directory. Each root is readable by anyone holding a session, so list the trees
# you keep code in rather than /.
# OMNIHARNESS_FS_ROOTS=/Users/you/code:/Volumes/External/code
#
# Persist or inject the 32-byte base64 auth encryption key. Back it up with the
# runner instance. If omitted, the runner creates .omniharness-auth.key.
# OMNIHARNESS_AUTH_KEY_PATH=/srv/omniharness/secrets/auth.key

# Optional: supervisor model configuration. These can also be set in Settings.
# SUPERVISOR_LLM_PROVIDER=gemini
# SUPERVISOR_LLM_MODEL=gemini-3.5-flash
# SUPERVISOR_LLM_API_KEY=
# SUPERVISOR_FALLBACK_LLM_PROVIDER=openai
# SUPERVISOR_FALLBACK_LLM_MODEL=gpt-5.4-mini
# SUPERVISOR_FALLBACK_LLM_API_KEY=

# Optional: agent provider API keys, if your local agent CLI is not already logged in.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=

# Optional Codex ACP package locations. Auto installs the maintained published
# npm adapter and the matching published Codex binary; it does not compile ACP.
# OMNIHARNESS_CODEX_ACP_INSTALL=auto
# OMNIHARNESS_CODEX_ACP_NPM_ROOT=/Users/you/.local/share/omniharness/codex-acp
# OMNIHARNESS_CODEX_ACP_INSTALL_DIR=/Users/you/.local/bin
# Optional fallback: run codex-acp through Docker or Podman instead.
# OMNIHARNESS_CODEX_ACP_DOCKER_IMAGE=omniharness/codex-acp:local
# OMNIHARNESS_DOCKER_BIN=podman

# Optional: generic external credential profiles for ACP workers.
# OMNIHARNESS_CREDENTIAL_PROFILES_DIR=.omniharness/credential-profiles
# OMNIHARNESS_CREDENTIAL_PROFILE_CLAUDE=claude
# OMNIHARNESS_CREDENTIAL_COMMAND_CLAUDE=/Users/you/.local/bin/baton
# OMNIHARNESS_CREDENTIAL_COMMAND_ARGS_CLAUDE=["credential-profile"]

# Unified worker conversation stream (Phase 4 cutover — default ON). Set
# to 0/false/off to revert to the legacy messages-table reader path
# while the rollout settles. The flag is removed entirely in Phase 5.
# OMNI_UNIFIED_WORKER_STREAM=1
